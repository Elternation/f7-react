import React     from 'react';
import PropTypes from 'prop-types';

class F7DialogTitle extends React.Component {
  render() {
    return null;
  }
}

F7DialogTitle.propTypes = {
  children: PropTypes.string.isRequired
};

export default F7DialogTitle;
